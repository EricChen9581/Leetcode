// HashMap + Double LinkedList
// LRU : Least Recently Used cache.
// It has 2 methods, the first is get, the other is put.
// Get: get the value of the key, if didn't exist, return -1;
// and after using, put the recently used pair into the first place.
// Put: Put the new key-value pair into the queue. if reached the capacity, firstly we need to drop the last element and add the new element.
//
